# Glossary (AUTHORING GUIDE)

> **Replace this table** with your project's terms. Include: domain terms (what's an
> "organization" vs a "workspace"? an "apotek" vs a "branch"?), internal identifiers agents will
> meet in code (`auth_sub`, `tenant_id`, …), acronyms, and **renamed concepts** (old name → new
> name, so historical references still resolve). One row each; dense.

# Glossary

> current to {{commit sha / date}}

| Term | Meaning |
|---|---|
| **{{Term}}** | {{definition — include the DB table/column or code symbol it maps to}} |
| **{{Old name}}** | Former name of {{new name}} ({{when renamed}}) — appears in old migrations/logs |
| **{{Acronym}}** | {{expansion + one-line meaning in this project}} |
