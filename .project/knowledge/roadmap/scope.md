# Scope (AUTHORING GUIDE)

> **Replace this file.** It declares what is and isn't part of this project — and it **overrides**
> every roadmap/backlog entry that suggests otherwise. Agents must confirm with the human before
> starting work on anything listed out of scope, *even if asked directly*. Write it early: it's
> the cheapest way to stop agents burning time on surfaces you don't maintain.

# Scope

> updated {{date}}

## In scope
*{{the surfaces/areas that ARE the project — e.g. "backend + web app + admin console". Must
match `MANIFEST.yaml → surfaces`.}}*

## Out of scope
| Area | State | Resume when |
|---|---|---|
| {{e.g. mobile app}} | {{removed / never started / frozen}} | {{the concrete condition, + "confirm with the human first"}} |

*{{If docs or history mention out-of-scope features, state explicitly that those references are
historical only.}}*

## Why this exists
*{{one paragraph: what keeping these out protects (focus, maintenance surface, template
reusability…).}}*
